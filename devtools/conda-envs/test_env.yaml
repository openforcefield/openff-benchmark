name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - pandas
  - dash
  - dash-bootstrap-components
  - dash-bio

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

