name: test

channels:
  - conda-forge
  - omnia
  - openeye

dependencies:
    # Base depends
  - python
  - pip
  - openforcefield
  - openeye-toolkits
  - cmiles
  - numpy

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - qcportal

    # Pip-only installs
  #- pip:
  #  - codecov

