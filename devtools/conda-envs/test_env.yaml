name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - openforcefield
  - cmiles
  - numpy

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - qcportal

    # Pip-only installs
  #- pip:
  #  - codecov

